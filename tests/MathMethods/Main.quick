func Main() Int
{
    val string = scope String.Create();

    val a = -6;

    string.Push.<Int>(a.Abs());
    string.Push.<Char>(' ');

    val b Float64 = -7.8;

    string.Push.<Float64>(b.Abs().Ceil());
    string.Push.<Char>(' ');

    return 0;
}